# UI Exercise Starter

## Goal
Build a minimal UI that talks to your robot API.

## TODO ideas
- Add loading and disabled states.
- Show command success/error status cleanly.
- Fetch and render full history from the API.
- Add a small board visualization (optional).

## Run
Open `index.html` in a browser, or serve this folder with any static server.
